#include <stdint.h>

// Variables that are in BE objects that aren't included in the emulated builds

uint8_t user_selected_can_addon_crystal_frequency_mhz = 0;
uint8_t user_selected_canfd_addon_crystal_frequency_mhz = 0;
